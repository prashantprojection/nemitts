import OverlaySettings from "./OverlaySettings";

/**
 * OBS Settings component
 * This component is used in the settings page to configure OBS overlay settings
 */
const ObsSettings = () => {
  return <OverlaySettings />;
};

export default ObsSettings;
